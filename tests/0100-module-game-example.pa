module Inventory {
    .items: int[list] // list of ints that represent items
    constrain items.length < 10 // player can hold a maximum of 10 items

    // drop one item from the inventory
    fn drop #TODO

    // pick up an item
    // ! Must handle constraint !
    fn pick_up self item_id =
        self with self.items = self.items :: item_id // !!! Raises error because constraint is not handled
}

type player {
    name: string,
    inventory: Inventory,

}